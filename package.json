{
  "name": "passline",
  "version": "0.1.0",
  "private": true,
  "description": "Pass Line — a play-money craps trainer",
  "scripts": {
    "bundle": "rm -rf dist && mkdir dist && cp game.html dist/index.html && cp manifest.webmanifest sw.js icon-192.png icon-512.png dist/",
    "sync": "npm run bundle && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/preferences": "^8.0.1",
    "@revenuecat/purchases-capacitor": "^13.4.2"
  }
}
